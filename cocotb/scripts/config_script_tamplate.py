clock        = replace by clock
max_err      = replace by max number of errer
sky          = replace sky enable
CARAVEL_ROOT = replace CARAVEL_ROOT
MCW_ROOT     = replace MCW_ROOT
PDK_ROOT     = replace PDK_ROOT
PDK          = replace PDK
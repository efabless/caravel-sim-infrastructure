---
# yaml file contain general design information that would mostly need to be updated in the first run only 
# example 
## tests: [debug,clock_redirect]
## sim: [RTL,RTL]
Tests: 
    - {name: debug_swd, sim: RTL}
    - {name: clock_redirect, sim: RTL}

includes: 
    - ../testlist2.yaml
